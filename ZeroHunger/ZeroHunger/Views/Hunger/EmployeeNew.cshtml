@model IEnumerable<ZeroHunger.DB.Emoplyee>

@{
    ViewBag.Title = "EmployeeNew";
}

<h2>EmployeeNew</h2>

<p>
    @Html.ActionLink("Create New", "Creat")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgoId)
        </td>
        <td>
            @Html.ActionLink("ASSign", "ASSign", new { id = item.Id }) |
        </td>
    </tr>
}

</table>
